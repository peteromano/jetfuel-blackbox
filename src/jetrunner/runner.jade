!!! 5
html(lang="en")
  head
    title= title
    each style in styles
      link(href="#{style}", rel="stylesheet")
  body
    each script in scripts
      script(src="#{script}")
    script(src="#{src}")
    script(src="#{test}")
    script
       mocha.ui('bdd');
       mocha.reporter('spec'); // spec, list, html, doc, tap, dot, min
       mocha.run();